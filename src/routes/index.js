import { BrowserRouter } from 'react-router-dom';
import App from '../containers/App';

const RoutesConfig = () => (
  <BrowserRouter>
    <App />
  </BrowserRouter>
);

export default RoutesConfig;
